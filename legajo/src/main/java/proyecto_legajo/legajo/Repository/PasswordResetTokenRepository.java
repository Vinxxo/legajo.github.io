package proyecto_legajo.legajo.Repository;

// Repositorio comentado: no usamos tabla de tokens, usamos JWT en PasswordResetService
// import org.springframework.data.jpa.repository.JpaRepository;
// import proyecto_legajo.legajo.Entity.PasswordResetToken;
// import proyecto_legajo.legajo.Entity.usuarios;

// public interface PasswordResetTokenRepository extends JpaRepository<PasswordResetToken, Long> {
//     PasswordResetToken findByToken(String token);
//     PasswordResetToken findByUsuario(usuarios usuario);
// }
